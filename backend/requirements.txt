# ===== 后端运行依赖 =====
# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.31.0

# ORM / 数据库
SQLAlchemy==2.0.51
PyMySQL==1.2.0          # MySQL 驱动（DATABASE_URL=mysql+pymysql://...）
alembic==1.18.5         # 数据库迁移

# 数据校验 / 配置
pydantic>=2.7,<3
pydantic-settings>=2.3
python-dotenv>=1.0      # pydantic-settings 读取 .env
email-validator==2.3.0  # Pydantic EmailStr 校验

# 认证 / 安全
PyJWT==2.13.0
bcrypt==5.0.0
python-multipart==0.0.12  # FastAPI 文件上传 / 表单

# 文件存储（可选，留空配置时使用本地存储）
minio==7.2.20

# Excel 导出
openpyxl>=3.1

# ===== 开发 / 测试 =====
pytest>=8.0
pytest-cov>=5.0
