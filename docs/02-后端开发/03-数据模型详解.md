# 数据模型详解

## 一、用户与权限模型

### 1.1 Department (部门)

**表名：** `departments`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, autoincrement | 主键 |
| name | String(100) | NOT NULL, UNIQUE | 部门名称 |
| parent_id | Integer | FK(departments.id), NULL | 上级部门 ID |
| created_at | DateTime | default=now | 创建时间 |

**关系：**
- `users` → 一对多，部门下的用户

**定义位置：** [user.py:8-17](file:///g:/资料/code/factory-pms/backend/app/models/user.py#L8-L17)

---

### 1.2 User (用户)

**表名：** `users`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, autoincrement | 主键 |
| username | String(64) | UNIQUE, NOT NULL, INDEX | 用户名 |
| email | String(200) | UNIQUE, NOT NULL, INDEX | 邮箱 |
| password_hash | String(128) | NOT NULL | 密码哈希 |
| real_name | String(100) | - | 真实姓名 |
| role | String(32) | default="member" | 角色：admin/manager/member/viewer |
| dept_id | Integer | FK(departments.id), NULL | 部门 ID |
| is_active | Boolean | default=True | 是否启用 |
| created_at | DateTime | default=now | 创建时间 |
| last_login_at | DateTime | NULL | 最后登录时间 |

**关系：**
- `department` → 多对一，所属部门
- `owned_projects` → 一对多，负责的项目

**定义位置：** [user.py:19-34](file:///g:/资料/code/factory-pms/backend/app/models/user.py#L19-L34)

---

## 二、项目管理模型

### 2.1 ProjectType (项目类型)

**表名：** `project_types`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK | 主键 |
| name | String(100) | NOT NULL, UNIQUE | 类型名称 |
| description | String(500) | - | 描述 |
| is_active | Boolean | default=True | 是否启用 |

**定义位置：** [project.py:8-15](file:///g:/资料/code/factory-pms/backend/app/models/project.py#L8-L15)

---

### 2.2 Project (项目)

**表名：** `projects`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| code | String(32) | UNIQUE, NOT NULL, INDEX | 项目编号 |
| name | String(200) | NOT NULL | 项目名称 |
| description | Text | - | 项目描述 |
| status | String(32) | default="draft", INDEX | 状态：draft/active/on_hold/completed/cancelled |
| priority | Integer | default=3 | 优先级 1-5 |
| project_type_id | Integer | FK(project_types.id), NULL | 项目类型 ID |
| owner_id | Integer | FK(users.id), NOT NULL | 负责人 ID |
| created_by | Integer | FK(users.id), NOT NULL | 创建人 ID |
| plan_start | Date | NULL | 计划开始日期 |
| plan_end | Date | NULL | 计划结束日期 |
| actual_start | Date | NULL | 实际开始日期 |
| actual_end | Date | NULL | 实际结束日期 |
| progress | Integer | default=0 | 进度 0-100 |
| budget | String(100) | NULL | 预算 |
| created_at | DateTime | default=now | 创建时间 |
| updated_at | DateTime | default=now, onupdate=now | 更新时间 |

**状态流转：**
```
draft → active → on_hold → completed
                ↓
             cancelled
```

**关系：**
- `owner` → 多对一，项目负责人
- `creator` → 多对一，创建人
- `tasks` → 一对多，项目任务
- `requirements` → 一对多，项目需求
- `experiments` → 一对多，关联实验
- `samples` → 一对多，关联样品

**定义位置：** [project.py:17-50](file:///g:/资料/code/factory-pms/backend/app/models/project.py#L17-L50)

---

### 2.3 Task (任务)

**表名：** `tasks`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| project_id | Integer | FK(projects.id), NOT NULL, INDEX | 项目 ID |
| parent_id | Integer | FK(tasks.id), NULL | 父任务 ID |
| title | String(500) | NOT NULL | 任务标题 |
| description | Text | - | 任务描述 |
| status | String(32) | default="todo", INDEX | 状态：todo/in_progress/review/done |
| priority | Integer | default=3 | 优先级 1-5 |
| assignee_id | Integer | FK(users.id), NULL | 负责人 ID |
| plan_hours | Integer | default=0 | 计划工时 |
| actual_hours | Integer | default=0 | 实际工时 |
| due_date | Date | NULL | 截止日期 |
| sort_order | Integer | default=0 | 排序 |
| created_at | DateTime | default=now | 创建时间 |
| updated_at | DateTime | default=now, onupdate=now | 更新时间 |

**状态流转：**
```
todo → in_progress → review → done
```

**关系：**
- `project` → 多对一，所属项目
- `parent` → 多对一，父任务
- `children` → 一对多，子任务

**定义位置：** [project.py:52-77](file:///g:/资料/code/factory-pms/backend/app/models/project.py#L52-L77)

---

### 2.4 Requirement (需求)

**表名：** `requirements`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| project_id | Integer | FK(projects.id), NOT NULL, INDEX | 项目 ID |
| code | String(32) | UNIQUE, NOT NULL | 需求编号 |
| title | String(500) | NOT NULL | 需求标题 |
| description | Text | - | 需求描述 |
| source | String(32) | default="internal" | 来源：customer/sales/internal/market |
| priority | String(16) | default="should" | 优先级：must/should/could/wont |
| status | String(32) | default="draft" | 状态：draft/review/approved/implemented/verified/rejected |
| version | Integer | default=1 | 版本号 |
| created_at | DateTime | default=now | 创建时间 |

**关系：**
- `project` → 多对一，所属项目

**定义位置：** [project.py:79-96](file:///g:/资料/code/factory-pms/backend/app/models/project.py#L79-L96)

---

## 三、实验管理模型

### 3.1 Experiment (实验)

**表名：** `experiments`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| project_id | Integer | FK(projects.id), NOT NULL, INDEX | 项目 ID |
| code | String(32) | UNIQUE, NOT NULL | 实验编号 |
| name | String(200) | NOT NULL | 实验名称 |
| description | Text | - | 实验描述 |
| exp_type | String(32) | NOT NULL | 实验类型：工艺验证/性能测试/材料试验/可靠性测试 |
| status | String(32) | default="draft" | 状态：draft/planned/running/completed/cancelled |
| designer_id | Integer | FK(users.id), NOT NULL | 设计人 ID |
| executor_id | Integer | FK(users.id), NULL | 执行人 ID |
| plan_start | Date | NULL | 计划开始 |
| plan_end | Date | NULL | 计划结束 |
| actual_start | Date | NULL | 实际开始 |
| actual_end | Date | NULL | 实际结束 |
| param_template | JSON | NULL | 参数模板 |
| created_at | DateTime | default=now | 创建时间 |
| updated_at | DateTime | default=now, onupdate=now | 更新时间 |

**param_template 结构示例：**
```json
[
  {"name": "温度", "unit": "℃", "default": 180},
  {"name": "压力", "unit": "MPa", "default": 12.5}
]
```

**关系：**
- `project` → 多对一，所属项目
- `records` → 一对多，实验记录
- `designer` → 多对一，设计人
- `executor` → 多对一，执行人

**定义位置：** [experiment.py:8-40](file:///g:/资料/code/factory-pms/backend/app/models/experiment.py#L8-L40)

---

### 3.2 ExperimentRecord (实验记录)

**表名：** `experiment_records`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| experiment_id | Integer | FK(experiments.id), NOT NULL, INDEX | 实验 ID |
| batch_no | String(50) | NULL | 批次号 |
| sample_code | String(50) | NULL | 样品编号 |
| executor_id | Integer | FK(users.id), NULL | 执行人 ID |
| param_values | JSON | NULL | 参数值 |
| result_data | JSON | NULL | 测试数据 |
| result_summary | Text | NULL | 结果摘要 |
| conclusion | String(32) | NULL | 结论：pass/fail/conditional_pass/need_retest |
| cpk | Float | NULL | CPK 值 |
| mean_value | Float | NULL | 平均值 |
| std_dev | Float | NULL | 标准差 |
| is_abnormal | Boolean | default=False | 是否异常 |
| abnormal_desc | Text | NULL | 异常描述 |
| executed_at | Date | NULL | 执行日期 |
| created_at | DateTime | default=now | 创建时间 |

**关系：**
- `experiment` → 多对一，所属实验
- `attachments` → 一对多，附件

**定义位置：** [experiment.py:43-75](file:///g:/资料/code/factory-pms/backend/app/models/experiment.py#L43-L75)

---

### 3.3 ExperimentAttachment (实验附件)

**表名：** `experiment_attachments`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK | 主键 |
| record_id | Integer | FK(experiment_records.id), NOT NULL | 记录 ID |
| file_name | String(255) | NOT NULL | 文件名 |
| file_path | String(500) | NOT NULL | 文件路径 |
| file_size | Integer | default=0 | 文件大小 |
| uploaded_at | DateTime | default=now | 上传时间 |

**定义位置：** [experiment.py:78-88](file:///g:/资料/code/factory-pms/backend/app/models/experiment.py#L78-L88)

---

## 四、BOM 管理模型

### 4.1 Material (物料)

**表名：** `materials`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| code | String(64) | UNIQUE, NOT NULL, INDEX | 物料编码 |
| name | String(200) | NOT NULL, INDEX | 物料名称 |
| spec | String(500) | NULL | 规格型号 |
| material_type | String(32) | NOT NULL, INDEX | 类型：raw_material/component/semi_finished/finished/auxiliary |
| unit | String(16) | default="个" | 单位 |
| category | String(100) | NULL | 分类 |
| supplier | String(200) | NULL | 供应商 |
| brand | String(100) | NULL | 品牌 |
| status | String(32) | default="active" | 状态：active/inactive/pending |
| created_by | Integer | FK(users.id), NULL | 创建人 ID |
| created_at | DateTime | default=now | 创建时间 |
| updated_at | DateTime | default=now, onupdate=now | 更新时间 |

**定义位置：** [bom.py:8-31](file:///g:/资料/code/factory-pms/backend/app/models/bom.py#L8-L31)

---

### 4.2 BomHeader (BOM 表头)

**表名：** `bom_headers`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| project_id | Integer | FK(projects.id), NULL, INDEX | 项目 ID |
| code | String(64) | UNIQUE, NOT NULL, INDEX | BOM 编号 |
| name | String(200) | NOT NULL | BOM 名称 |
| description | Text | NULL | 描述 |
| version | String(20) | default="V1.0" | 版本 |
| status | String(32) | default="draft" | 状态：draft/review/released/archived/obsolete |
| product_code | String(64) | NULL | 关联产品编码 |
| remark | Text | NULL | 备注 |
| created_by | Integer | FK(users.id), NULL | 创建人 ID |
| approved_by | Integer | FK(users.id), NULL | 审批人 ID |
| created_at | DateTime | default=now | 创建时间 |
| updated_at | DateTime | default=now, onupdate=now | 更新时间 |

**关系：**
- `items` → 一对多，BOM 子项
- `change_logs` → 一对多，变更记录

**定义位置：** [bom.py:34-58](file:///g:/资料/code/factory-pms/backend/app/models/bom.py#L34-L58)

---

### 4.3 BomItem (BOM 子项)

**表名：** `bom_items`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| bom_id | Integer | FK(bom_headers.id), NOT NULL, INDEX | BOM ID |
| material_id | Integer | FK(materials.id), NOT NULL, INDEX | 物料 ID |
| line_no | Integer | default=1 | 行号 |
| quantity | String(50) | NOT NULL | 用量（支持表达式） |
| unit | String(16) | NULL | 单位 |
| loss_rate | String(20) | default="0" | 损耗率 |
| level | Integer | default=1 | BOM 层级 |
| parent_item_id | Integer | FK(bom_items.id), NULL | 父项 ID |
| remark | String(500) | NULL | 备注 |
| is_key | Boolean | default=False | 是否关键物料 |
| created_at | DateTime | default=now | 创建时间 |

**定义位置：** [bom.py:61-82](file:///g:/资料/code/factory-pms/backend/app/models/bom.py#L61-L82)

---

### 4.4 BomChange (BOM 变更)

**表名：** `bom_changes`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| bom_id | Integer | FK(bom_headers.id), NOT NULL, INDEX | BOM ID |
| change_type | String(32) | NOT NULL | 类型：ECR/ECN/manual |
| change_no | String(64) | UNIQUE, NOT NULL | 变更单号 |
| title | String(200) | NOT NULL | 变更标题 |
| reason | Text | NULL | 变更原因 |
| description | Text | NULL | 变更描述 |
| status | String(32) | default="draft" | 状态：draft/review/approved/implemented/rejected |
| applicant_id | Integer | FK(users.id), NULL | 申请人 ID |
| reviewer_id | Integer | FK(users.id), NULL | 审核人 ID |
| implemented_at | DateTime | NULL | 实施时间 |
| created_at | DateTime | default=now | 创建时间 |
| updated_at | DateTime | default=now, onupdate=now | 更新时间 |

**定义位置：** [bom.py:84-107](file:///g:/资料/code/factory-pms/backend/app/models/bom.py#L84-L107)

---

## 五、样品试产模型

### 5.1 Sample (样品)

**表名：** `samples`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| project_id | Integer | FK(projects.id), NOT NULL, INDEX | 项目 ID |
| sample_no | String(64) | UNIQUE, NOT NULL, INDEX | 样品编号 |
| name | String(200) | NOT NULL | 样品名称 |
| description | Text | NULL | 描述 |
| version | String(20) | default="V1.0" | 版本 |
| status | String(32) | default="draft" | 状态：draft/making/testing/passed/failed/rework |
| sample_type | String(32) | default="development" | 类型：development/verification/pre_production |
| quantity | Integer | default=1 | 数量 |
| maker_id | Integer | FK(users.id), NULL | 制作人 ID |
| inspector_id | Integer | FK(users.id), NULL | 检验人 ID |
| plan_finish | Date | NULL | 计划完成 |
| actual_finish | Date | NULL | 实际完成 |
| test_result | String(32) | NULL | 测试结果：pending/pass/fail/conditional_pass |
| remark | Text | NULL | 备注 |
| created_by | Integer | FK(users.id), NULL | 创建人 ID |
| created_at | DateTime | default=now | 创建时间 |
| updated_at | DateTime | default=now, onupdate=now | 更新时间 |

**定义位置：** [sample.py:8-40](file:///g:/资料/code/factory-pms/backend/app/models/sample.py#L8-L40)

---

### 5.2 SampleInspection (样品检验)

**表名：** `sample_inspections`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| sample_id | Integer | FK(samples.id), NOT NULL, INDEX | 样品 ID |
| inspect_no | String(64) | UNIQUE, NOT NULL | 检验单号 |
| inspect_type | String(32) | NOT NULL | 类型：dimension/performance/appearance/reliability/full |
| inspector_id | Integer | FK(users.id), NULL | 检验人 ID |
| inspected_at | Date | NULL | 检验日期 |
| result | String(32) | NULL | 结果：pass/fail/conditional |
| dimension_data | JSON | NULL | 尺寸检测数据 |
| performance_data | JSON | NULL | 性能检测数据 |
| appearance_desc | Text | NULL | 外观检查描述 |
| failure_desc | Text | NULL | 不合格描述 |
| disposition | String(32) | NULL | 处置：accept/rework/scrap/use_as_is |
| remark | Text | NULL | 备注 |
| created_at | DateTime | default=now | 创建时间 |

**定义位置：** [sample.py:43-71](file:///g:/资料/code/factory-pms/backend/app/models/sample.py#L43-L71)

---

### 5.3 TrialProduction (试产工单)

**表名：** `trial_productions`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| project_id | Integer | FK(projects.id), NOT NULL, INDEX | 项目 ID |
| trial_no | String(64) | UNIQUE, NOT NULL, INDEX | 试产单号 |
| name | String(200) | NOT NULL | 试产名称 |
| bom_id | Integer | FK(bom_headers.id), NULL | BOM ID |
| sample_id | Integer | FK(samples.id), NULL | 样品 ID |
| status | String(32) | default="planned" | 状态：planned/in_progress/completed/aborted |
| plan_qty | Integer | default=0 | 计划数量 |
| actual_qty | Integer | default=0 | 实际数量 |
| pass_qty | Integer | default=0 | 合格数量 |
| fail_qty | Integer | default=0 | 不合格数量 |
| plan_start | Date | NULL | 计划开始 |
| plan_end | Date | NULL | 计划结束 |
| actual_start | Date | NULL | 实际开始 |
| actual_end | Date | NULL | 实际结束 |
| workshop | String(100) | NULL | 车间 |
| line_no | String(50) | NULL | 产线 |
| foreman_id | Integer | FK(users.id), NULL | 领班 ID |
| process_params | JSON | NULL | 工艺参数 |
| issue_desc | Text | NULL | 问题记录 |
| yield_rate | Float | NULL | 良率 |
| conclusion | String(32) | NULL | 结论：ready_for_mass/need_improvement/fail |
| remark | Text | NULL | 备注 |
| created_by | Integer | FK(users.id), NULL | 创建人 ID |
| created_at | DateTime | default=now | 创建时间 |
| updated_at | DateTime | default=now, onupdate=now | 更新时间 |

**定义位置：** [sample.py:91-129](file:///g:/资料/code/factory-pms/backend/app/models/sample.py#L91-L129)

---

## 六、文档知识模型

### 6.1 Document (文档)

**表名：** `documents`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| code | String(64) | UNIQUE, NOT NULL, INDEX | 文档编号 |
| title | String(200) | NOT NULL, INDEX | 文档标题 |
| doc_type | String(32) | NOT NULL, INDEX | 类型：design/test/process/quality/standard/other |
| category_id | Integer | FK(doc_categories.id), NULL | 分类 ID |
| project_id | Integer | FK(projects.id), NULL, INDEX | 项目 ID |
| current_version | String(20) | default="V1.0" | 当前版本 |
| status | String(32) | default="draft" | 状态：draft/review/approved/archived/obsolete |
| tags | JSON | NULL | 标签列表 |
| summary | Text | NULL | 摘要 |
| created_by | Integer | FK(users.id), NULL | 创建人 ID |
| created_at | DateTime | default=now | 创建时间 |
| updated_at | DateTime | default=now, onupdate=now | 更新时间 |

**定义位置：** [document.py:8-34](file:///g:/资料/code/factory-pms/backend/app/models/document.py#L8-L34)

---

### 6.2 KnowledgeArticle (知识文章)

**表名：** `knowledge_articles`

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | Integer | PK, INDEX | 主键 |
| title | String(200) | NOT NULL, INDEX | 文章标题 |
| content | Text | NULL | Markdown 内容 |
| category | String(100) | NULL | 分类 |
| tags | JSON | NULL | 标签 |
| is_published | Boolean | default=False | 是否发布 |
| view_count | Integer | default=0 | 浏览量 |
| author_id | Integer | FK(users.id), NULL | 作者 ID |
| created_at | DateTime | default=now | 创建时间 |
| updated_at | DateTime | default=now, onupdate=now | 更新时间 |

**定义位置：** [document.py:83-99](file:///g:/资料/code/factory-pms/backend/app/models/document.py#L83-L99)
